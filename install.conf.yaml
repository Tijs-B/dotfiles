- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf:

    ~/.vim:
    ~/.vimrc:

    ~/.shell:

    ~/.zsh:
    ~/.zshrc:

    ~/.bashrc:

    ~/.gitignore_global:
    ~/.gitconfig:


- shell:
  - [git submodule update --init --recursive, Installing submodules]
