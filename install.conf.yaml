- defaults:
    link:
      relink: true
      create: true

- clean: ["~"]

- link:
    ~/.tmux.conf:

    ~/.shell:

    ~/.zshrc:

    ~/.bashrc:

    ~/.gitignore_global:
    ~/.gitconfig:

    ~/.hushlogin:

    ~/.config/bat: bat
    ~/.config/nvim: nvim

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - [
        git clean -fdx
        zsh/plugins/ firefox/chrome/,
        Cleaning submodules,
      ]
