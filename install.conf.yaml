- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf:

    ~/.vim:
    ~/.vimrc:

    ~/.shell:

    ~/.zsh:
    ~/.zshrc:

    ~/.bashrc:



- shell:
  - [git submodule update --init --recursive, Installing submodules]
